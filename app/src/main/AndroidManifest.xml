<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dementiaapp">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DementiaApp">

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.SetReminderActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".presentation.GetReminderActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".presentation.AboutMeActivity"
            android:exported="true">
        </activity>

        <receiver
            android:name=".presentation.ReminderReceiver"
            android:exported="true">
        </receiver>

    </application>
</manifest>
